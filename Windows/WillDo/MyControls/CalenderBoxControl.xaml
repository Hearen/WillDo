<UserControl x:Class="WillDo.MyControls.CalendarBoxControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="titleTextBlock" Grid.Row="0"/>
        <DatePicker x:Name="dateSelector" SelectedDateChanged="dateSelector_SelectedDateChanged" Grid.Row="1" Height="25" SelectedDateFormat ="Short" FirstDayOfWeek="Sunday"/>
    </Grid>
</UserControl>
